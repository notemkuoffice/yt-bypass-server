services:
  - type: web
    name: yt-bypass-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    region: frankfurt # Выбираем Франкфурт для лучшего пинга из РФ
